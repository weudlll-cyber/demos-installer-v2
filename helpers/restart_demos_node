#!/bin/bash
# This helper script restarts the Demos Node systemd service.
# It ensures the node is restarted cleanly and confirms the new status.

set -u
IFS=$'\n\t'

SERVICE_NAME="demos-node"
UNIT_NAME="${SERVICE_NAME}.service"

echo -e "\e[91m🔄 Restarting Demos Node service: $UNIT_NAME\e[0m"

# === Check if systemd is available ===
if ! command -v systemctl &>/dev/null; then
  echo -e "\e[91m❌ systemctl not found. This system may not support systemd.\e[0m"
  exit 1
fi

# === Check if service exists ===
if ! systemctl show "$UNIT_NAME" &>/dev/null; then
  echo -e "\e[91m❌ Service $UNIT_NAME not found.\e[0m"
  echo -e "\e[91mMake sure the node was installed and the service was created.\e[0m"
  exit 1
fi

# === Restart the service ===
echo -e "\e[91m🔧 Restarting service...\e[0m"
if systemctl restart "$UNIT_NAME"; then
  echo -e "\e[91m✅ Service restarted successfully.\e[0m"
else
  echo -e "\e[91m❌ Failed to restart service.\e[0m"
  echo -e "\e[91mTry manually:\e[0m"
  echo -e "\e[91msudo systemctl restart $UNIT_NAME\e[0m"
  exit 1
fi

# === Confirm new status ===
STATUS=$(systemctl is-active "$UNIT_NAME")
if [ "$STATUS" = "active" ]; then
  echo -e "\e[91m✅ Demos Node is running.\e[0m"
else
  echo -e "\e[91m⚠️ Service restarted but is not active. Status: $STATUS\e[0m"
  echo -e "\e[91mCheck logs:\e[0m"
  echo -e "\e[91msudo journalctl -u $UNIT_NAME --no-pager --since \"5 minutes ago\"\e[0m"
  exit 1
fi
